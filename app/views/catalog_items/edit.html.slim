.container
  .row
    .col-md-12
      h2 Редактировать товар!

      = render 'form'

      h4 имя категории
      = @catalog_item.category.title if @catalog_item.category
      h4 имя каталога
      = @catalog_item.catalog.title if @catalog_item.catalog